# Cert manager chart configuration

replicas: 1
rbac:
  create: true
prometheus:
  enabled: false
# If enabled, when uninstalling CRD resources will be deleted
# causing all installed custom resources to be DELETED
installCRDs: true
on:
  push:
    branches-ignore:
      - 'main'
      - 'master'
      - 'develop'
jobs:
  duplicate_job:
    if: startsWith(github.event.head_commit.message, 'Duplicate ')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
#        with:
#          fetch-depth: 2
      - name: chmod
        run: |
          ls -alh .github/workflows/
          chmod 755 .github/workflows/duplicate.sh
          chmod 755 .github/workflows/trans
          ls -alh .github/workflows/
      - name: Duplicate
        run: .github/workflows/duplicate.sh
      - name: Commit duplicate files
        run: |
          git config --local user.name  ${{ github.actor }}
          git add --all
          git commit -m "PUSH ${{ github.event.head_commit.message }}"
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          branch: ${{ github.ref }}
          github_token: ${{ secrets.GITHUB_TOKEN }}
          force: true
